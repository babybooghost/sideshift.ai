{
  "name": "sideshift-ai",
  "version": "0.1.2",
  "description": "Screen-analysis AI overlay. Highlight anything on screen, open focused side-chats. App-agnostic, uses your own Anthropic API key.",
  "private": true,
  "author": "babybooghost",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "fable": "dotnet fable src --outDir dist-fable --run vite build",
    "fable:watch": "dotnet fable watch src --outDir dist-fable --run vite",
    "vite": "vite",
    "vite:build": "vite build",
    "dev": "concurrently -k \"npm:fable:watch\" \"npm:electron:dev\"",
    "electron": "electron .",
    "electron:dev": "cross-env NODE_ENV=development electron .",
    "build": "dotnet fable src --outDir dist-fable && vite build",
    "start": "npm run build && electron .",
    "icon": "electron build/render-icon.mjs build/icon.svg build/icon.png 1024",
    "dist": "npm run build && electron-builder -mw",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:win": "npm run build && electron-builder --win"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.0.3"
  }
}
